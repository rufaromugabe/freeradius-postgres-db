# FreeRADIUS Environment Configuration
# 
# IMPORTANT: Copy this file to .env and customize as needed
# WARNING: Change all default passwords before deploying to production!
# 
# Usage:
#   cp .env.example .env
#   nano .env  # Edit and change passwords

# PostgreSQL Database Configuration
POSTGRES_DB=radius
POSTGRES_USER=radius
POSTGRES_PASSWORD=radiuspassword

# Database connection for FreeRADIUS
DB_HOST=postgres
DB_PORT=5432
DB_NAME=radius
DB_USER=radius
DB_PASSWORD=radiuspassword

# FreeRADIUS Configuration
# Set to 'yes' to start in debug mode (-X)
RADIUS_DEBUG=no

# Timezone
TZ=UTC

# Network Configuration
RADIUS_AUTH_PORT=1812
RADIUS_ACCT_PORT=1813